apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "k8s-logging-agent.fullname" . }}-collector
  labels:
    {{- include "k8s-logging-agent.labels" . | nindent 4 }}
data:
  config.yaml: |
{{ tpl .Values.collector.config . | nindent 4 }}
